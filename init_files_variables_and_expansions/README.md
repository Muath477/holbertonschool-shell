# Shell, init files, variables and expansions

This directory contains shell scripts for the init files, variables and expansions project.

## Tasks

* `0-alias`: Creates an alias named `ls` with the value `rm -f *`
